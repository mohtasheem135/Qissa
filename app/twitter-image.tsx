/**
 * Reuses the root Open Graph image as the Twitter card image. Twitter's
 * `summary_large_image` card uses the same 1200×630 aspect ratio.
 */
export { default, alt, contentType, size } from "./opengraph-image";
