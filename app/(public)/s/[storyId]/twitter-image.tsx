/**
 * Reuses the per-story Open Graph image as the Twitter card image. Twitter's
 * `summary_large_image` card uses the same aspect ratio (1200×630) so a
 * single render covers both surfaces.
 */
export { default, alt, contentType, size } from "./opengraph-image";
